#!/usr/bin/env bash

BIN_DIR=$(cd $(dirname $0); pwd)

# run image-server in background
nohup $BIN_DIR/image-server &

# main web proc
java -Denv=production \
  -Dstats_enabled=false \
  -Dport=$PORT \
  -cp target/laph-jar-with-dependencies.jar \
  laph.App